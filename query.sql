CREATE TABLE `codal`.`tableforchat` (
  `num` INT NOT NULL AUTO_INCREMENT,
  `chatuser` VARCHAR(30) NULL,
  `chattext` VARCHAR(1000) NULL,
  `chatpassword` VARCHAR(10) NULL,
  `chattime` VARCHAR(50) NULL,
  PRIMARY KEY (`num`));
